// Simple script to demonstrate functionality
document.addEventListener('DOMContentLoaded', () => {
    console.log('Hello World app loaded');

    // Optional: Add some dynamic behavior for signage
    const colors = ['#00b7af', '#ffffff', '#6a7071'];
    let colorIndex = 0;

    // Just a small log to verify JS is running
    setInterval(() => {
        // We could cycle colors or do something else here if needed
        // For a simple hello world, we'll keep it static as per CSS animation
    }, 5000);
});
